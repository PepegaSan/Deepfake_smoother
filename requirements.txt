# --- Runtime (Control Center, Flickercheck, compare from source) ---
# Python 3.10+ recommended (Windows wheels for OpenCV / tkinterdnd2).

opencv-python>=4.8.0,<5
numpy>=1.24.0,<3
Pillow>=10.0.0

customtkinter>=5.2.0
darkdetect>=0.8.0

# Tools tab: drag & drop from Explorer (gui.py / gui.exe — PyInstaller bundles tkdnd; see packaging/gui.spec)
tkinterdnd2>=0.4.0
